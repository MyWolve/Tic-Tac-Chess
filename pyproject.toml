[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Tic-Tac-Chess"
version = "0.1.0"
description = "A hybrid Tic-Tac-Chess game engine"

[tool.unittest]
discovery = "tests"
pattern = "*_test.py"
